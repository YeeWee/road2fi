---
phase: 04-content-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/gen/page.tsx
  - app/gen/GenForm.tsx
requirements:
  - GEN-01
  - GEN-02
  - GEN-08
  - GEN-09
  - GEN-10
autonomous: true

must_haves:
  truths:
    - "User can visit /gen directly via URL but cannot reach it from any nav link"
    - "User can enter a URL into an input field and submit the form"
    - "Loading state appears after form submission"
    - "Preview area is displayed when generation result is available"
  artifacts:
    - path: "app/gen/page.tsx"
      provides: "Hidden /gen route, Server Component shell"
      min_lines: 10
    - path: "app/gen/GenForm.tsx"
      provides: "Client component with URL input, loading state, preview area"
      exports: ["default function GenForm"]
  key_links:
    - from: "app/gen/GenForm.tsx"
      to: "app/gen/page.tsx"
      via: "import in page"
      pattern: "import.*GenForm.*from"

user_setup:
  - service: OpenAI-compatible API
    why: "Content generation requires an LLM API key (GEN-04, GEN-05)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "Your LLM provider dashboard (OpenAI, SiliconFlow, etc.)"
      - name: OPENAI_BASE_URL
        source: "Optional — only needed for non-OpenAI providers (e.g., https://api.siliconflow.cn/v1)"
      - name: OPENAI_MODEL
        source: "Optional — defaults to gpt-4o if not set"
    dashboard_config:
      - task: "Create API key in your provider dashboard"
        location: "Provider dashboard -> API Keys -> Create new key"
---

<objective>
Create the hidden `/gen` page at `app/gen/page.tsx` with a URL input form that accepts YouTube and article URLs. The page is NOT linked from the navigation (GEN-01, GEN-10). Includes loading state display during generation (GEN-08) and a preview panel for generated content (GEN-09).

Purpose: Provide the UI entry point for the entire content generation pipeline. The form will wire to a server action (created in later plans) that handles scraping, generation, and saving.

Output: `app/gen/page.tsx` (server component), `app/gen/GenForm.tsx` (client component)
</objective>

<execution_context>
@/Users/hw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/hw/workspaces/projects/websites/road2fi/.planning/ROADMAP.md
@/Users/hw/workspaces/projects/websites/road2fi/.planning/phases/04-content-generator/04-RESEARCH.md
@/Users/hw/workspaces/projects/websites/road2fi/app/layout.tsx
@/Users/hw/workspaces/projects/websites/road2fi/app/blog/page.tsx

# Key types from existing codebase
# From lib/posts.ts:
```typescript
export interface PostMeta {
  title: string
  date: string
  excerpt: string
  thumbnail: string
  slug: string
}
```

# Client component pattern: 'use client' directive at top of file
# Server component pattern: no directive, default for page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hidden /gen page shell (GEN-01, GEN-10)</name>
  <read_first>
    - app/layout.tsx (layout wrapper pattern)
    - app/blog/page.tsx (existing page.tsx structure for reference)
  </read_first>
  <files>app/gen/page.tsx</files>
  <acceptance_criteria>
    - File `app/gen/page.tsx` exists and is NOT inside `app/(nav)` or similar grouped route
    - File does NOT contain `'use client'` directive (it is a Server Component)
    - File exports `default function GenPage` or `export default function`
    - File imports and renders `GenForm` component
    - No navigation link to `/gen` is added in `components/layout/Header.tsx` or `components/layout/Footer.tsx`
    - File contains `import GenForm from './GenForm'`
  </acceptance_criteria>
  <action>
    Create `app/gen/page.tsx` as a Server Component (no `'use client'` directive). This is the hidden generator page shell.

    File content:
    ```tsx
    import GenForm from './GenForm'

    export default function GenPage() {
      return (
        <div className="mx-auto max-w-2xl px-4 py-12">
          <h1 className="mb-2 text-3xl font-bold">Blog Generator</h1>
          <p className="mb-8 text-gray-600">
            Enter a YouTube video URL or article link to generate a blog post.
          </p>
          <GenForm />
        </div>
      )
    }
    ```

    Do NOT add any navigation links pointing to `/gen` in Header or Footer components. The page must only be accessible by directly visiting `/gen`.
  </action>
  <verify>
    <automated>test -f app/gen/page.tsx && grep -c "GenForm" app/gen/page.tsx | grep -v '^#' | grep -q '[1-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Gen page exists at /gen, renders GenForm, has no nav links to it</done>
</task>

<task type="auto">
  <name>Task 2: Create GenForm client component with URL input, loading state, and preview (GEN-02, GEN-08, GEN-09)</name>
  <read_first>
    - app/gen/GenForm.tsx (file being created)
    - app/layout.tsx (styling patterns, Tailwind classes)
  </read_first>
  <files>app/gen/GenForm.tsx</files>
  <acceptance_criteria>
    - File `app/gen/GenForm.tsx` exists
    - File starts with `'use client'` directive on line 1
    - File contains `useState` hook usage (imported from 'react')
    - File contains `<input` element with `type="url"` or `type="text"` and a `placeholder` attribute referencing YouTube or article URL
    - File contains a `<button` element with text "Generate" or similar
    - File contains loading state logic: checks `loading` or `isLoading` state variable and renders different UI
    - File contains a preview section that conditionally renders when result/preview data exists
    - File exports `default function GenForm`
    - File does NOT import or reference any `openai`, `cheerio`, `youtube-transcript`, or `.env` values (client-side must not touch server code)
  </acceptance_criteria>
  <action>
    Create `app/gen/GenForm.tsx` as a Client Component (`'use client'` on line 1).

    This component manages:
    1. **URL input form**: A text input accepting URLs, with basic client-side validation (check URL format with `try { new URL(url) }`)
    2. **Loading state**: After form submission, show a loading indicator (e.g., "Generating..." text with a spinner or disabled button)
    3. **Error display**: Show error messages when generation fails
    4. **Preview panel**: Display generated content (title, excerpt, full content) after successful generation, with a "Save" button and "Discard" button

    Component structure:
    ```tsx
    'use client'
    import { useState } from 'react'

    interface GeneratedPost {
      title: string
      content: string
      excerpt: string
      thumbnail: string
      slug: string
    }

    export default function GenForm() {
      const [url, setUrl] = useState('')
      const [loading, setLoading] = useState(false)
      const [error, setError] = useState<string | null>(null)
      const [result, setResult] = useState<GeneratedPost | null>(null)

      async function handleSubmit(e: React.FormEvent) { ... }
      async function handleSave() { ... }
      function handleDiscard() { ... }

      // Render: form -> loading -> preview -> save/discard
    }
    ```

    **handleSubmit**: Validate URL format with `new URL(url)`, set loading=true, clear errors, call server action (function `generatePost(url)` imported from `./actions` — this file will be created in later plans, use `// @ts-ignore` or declare the function for now).

    **Loading state**: When `loading` is true, show `<p className="text-gray-500">Generating...</p>` and disable the form input/button.

    **Preview panel**: When `result` is not null, render the post title as `<h2>`, excerpt as `<p>`, and content in a `<div>` with readable typography. Show two buttons: "Save Post" (calls `handleSave`) and "Discard" (calls `handleDiscard`).

    **handleSave**: Call server action `savePostAction(result)` — imported from `./actions` (will exist in later plans).

    **handleDiscard**: Set `result` to null, clear form.

    Use Tailwind classes consistent with the project: `mx-auto`, `max-w-2xl`, `px-4`, `py-12`, `text-3xl`, `font-bold`, `text-gray-600`, etc.

    IMPORTANT: This is a client component. Do NOT import any server-only modules (cheerio, youtube-transcript, openai, fs, path). Do NOT reference process.env. All server logic stays in Server Actions.
  </action>
  <verify>
    <automated>test -f app/gen/GenForm.tsx && head -1 app/gen/GenForm.tsx | grep -q "'use client'" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>GenForm exists as client component with URL input, loading state, preview panel, save/discard buttons</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server-action | URL input crosses from untrusted client to server-side scraping |
| client→display | Generated content from LLM displayed in client preview |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | GenForm URL input | mitigate | Client-side URL format validation with `new URL()` before submission (GEN-02) |
| T-04-02 | Information Disclosure | GenForm.tsx | mitigate | No API keys, env vars, or server imports in client component — verified by acceptance criteria grep |
| T-04-03 | Tampering | /gen route visibility | accept | Hidden page not linked in nav; no auth required per GEN-10 (low-value target, internal tool) |
</threat_model>

<verification>
- [ ] `/gen` page loads without errors when visiting `http://localhost:3000/gen`
- [ ] No link to `/gen` appears in the header or footer navigation
- [ ] URL input accepts both `https://www.youtube.com/watch?v=...` and `https://example.com/article` formats
- [ ] Submitting a valid URL shows loading state
- [ ] Error message displays for invalid URLs or failed generation
- [ ] Preview panel shows generated content with Save and Discard buttons
</verification>

<success_criteria>
- `/gen` page renders at the route without errors
- Navigation contains zero references to `/gen`
- URL input field with placeholder mentioning YouTube/article URLs
- Form submission triggers loading state visually
- Preview panel displays generated post title, excerpt, and content
- Save and Discard buttons are present in preview state
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-generator/04-01-SUMMARY.md`
</output>
