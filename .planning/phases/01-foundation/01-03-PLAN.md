---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app/layout.tsx
  - components/layout/Header.tsx
  - components/layout/Footer.tsx
autonomous: true
requirements:
  - FOUND-03

must_haves:
  truths:
    - "Header component renders on every page via root layout"
    - "Footer component renders on every page via root layout"
    - "Navigation links include Home and Blog routes"
    - "Footer contains copyright text and essential links"
  artifacts:
    - path: "components/layout/Header.tsx"
      provides: "Global navigation header with site name and links"
      exports: ["default"]
    - path: "components/layout/Footer.tsx"
      provides: "Global footer with copyright, contact, links"
      exports: ["default"]
    - path: "app/layout.tsx"
      provides: "Root layout importing and rendering Header + Footer"
      contains: "Header"
  key_links:
    - from: "app/layout.tsx"
      to: "components/layout/Header.tsx"
      via: "import and render"
      pattern: "import.*Header.*from"
    - from: "app/layout.tsx"
      to: "components/layout/Footer.tsx"
      via: "import and render"
      pattern: "import.*Footer.*from"
---

<objective>
Set up global layout with navigation header (Header.tsx) and footer (Footer.tsx) components, integrated into root layout.

Purpose: Ensure every page in the application displays consistent navigation and branding through the root layout wrapper.
Output: Header.tsx, Footer.tsx components, updated layout.tsx wrapping children with Header + Footer
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md (component boundaries, directory structure)
@.planning/PROJECT.md (FIRE theme design guidelines)
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header and Footer layout components</name>
  <files>components/layout/Header.tsx, components/layout/Footer.tsx</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (component boundaries — layout/ directory)
    - .planning/PROJECT.md (FIRE theme: professional minimal, clean typography)
    - CLAUDE.md (FIRE theme design: minimal palette, high contrast)
  </read_first>
  <action>
    Create components/layout/Header.tsx as a Server Component:
    ```tsx
    import Link from "next/link"

    export default function Header() {
      return (
        <header className="border-b border-gray-200 bg-white">
          <nav className="mx-auto flex max-w-5xl items-center justify-between p-4 sm:px-6 lg:px-8">
            <Link href="/" className="text-xl font-bold tracking-tight text-gray-900">
              Road to Fi
            </Link>
            <div className="flex gap-6 text-sm text-gray-600">
              <Link href="/" className="hover:text-gray-900">
                Home
              </Link>
              <Link href="/blog" className="hover:text-gray-900">
                Blog
              </Link>
            </div>
          </nav>
        </header>
      )
    }
    ```

    Create components/layout/Footer.tsx as a Server Component:
    ```tsx
    import Link from "next/link"

    export default function Footer() {
      const year = new Date().getFullYear()
      return (
        <footer className="border-t border-gray-200 bg-gray-50">
          <div className="mx-auto max-w-5xl px-4 py-8 sm:px-6 lg:px-8">
            <div className="flex flex-col items-center gap-4 text-sm text-gray-600 sm:flex-row sm:justify-between">
              <p>&copy; {year} Road to Fi. All rights reserved.</p>
              <div className="flex gap-4">
                <Link href="/blog" className="hover:text-gray-900">
                  Blog
                </Link>
              </div>
            </div>
          </div>
        </footer>
      )
    }
    ```

    Both components are Server Components (no 'use client' directive) because they contain no interactivity.
    Header uses Link from next/link for client-side navigation.
    Footer dynamically generates the current year.
  </action>
  <verify>
    <automated>grep -q "export default function Header" components/layout/Header.tsx && grep -q 'href="/"' components/layout/Header.tsx && grep -q 'href="/blog"' components/layout/Header.tsx && grep -q "export default function Footer" components/layout/Footer.tsx && grep -q "All rights reserved" components/layout/Footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/layout/Header.tsx contains "export default function Header"
    - components/layout/Header.tsx contains 'href="/"' (home link)
    - components/layout/Header.tsx contains 'href="/blog"' (blog link)
    - components/layout/Header.tsx does NOT contain "use client" (Server Component)
    - components/layout/Footer.tsx contains "export default function Footer"
    - components/layout/Footer.tsx contains "All rights reserved"
    - components/layout/Footer.tsx contains "getFullYear()" for dynamic year
    - components/layout/Footer.tsx does NOT contain "use client" (Server Component)
  </acceptance_criteria>
  <done>
    Header displays site name + navigation links (Home, Blog). Footer displays copyright with dynamic year + essential links. Both are Server Components.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Header and Footer into root layout</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (current content from Plan 01)
    - components/layout/Header.tsx (just created)
    - components/layout/Footer.tsx (just created)
  </read_first>
  <action>
    Update app/layout.tsx to import and wrap children with Header and Footer:
    ```tsx
    import type { Metadata } from "next"
    import "./globals.css"
    import Header from "@/components/layout/Header"
    import Footer from "@/components/layout/Footer"

    export const metadata: Metadata = {
      title: "Road to Fi — Financial Independence",
      description: "Helping ordinary people achieve Financial Independence through clear, actionable investment insights.",
    }

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <html lang="en">
          <body>
            <Header />
            <main>{children}</main>
            <Footer />
          </body>
        </html>
      )
    }
    ```

    Key changes from Plan 01 version:
    - Import Header from "@/components/layout/Header"
    - Import Footer from "@/components/layout/Footer"
    - Wrap body content with Header before children, Footer after children
    - Wrap children in <main> element for semantic HTML
  </action>
  <verify>
    <automated>grep -q 'import Header from "@/components/layout/Header"' app/layout.tsx && grep -q 'import Footer from "@/components/layout/Footer"' app/layout.tsx && grep -q "<Header />" app/layout.tsx && grep -q "<Footer />" app/layout.tsx && grep -q "<main>" app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx contains 'import Header from "@/components/layout/Header"'
    - app/layout.tsx contains 'import Footer from "@/components/layout/Footer"'
    - app/layout.tsx contains "<Header />" in JSX
    - app/layout.tsx contains "<Footer />" in JSX
    - app/layout.tsx wraps children in <main> element
    - app/layout.tsx still contains 'lang="en"' on html element
    - app/layout.tsx still exports metadata
  </acceptance_criteria>
  <done>
    Root layout wraps all page content with Header above and Footer below. Semantic <main> element used. All pages inherit this layout automatically.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout→pages | Header/Footer rendered on all pages; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Spoofing | Header navigation links | mitigate | Use next/link for internal routing (prevents open redirect); all hrefs are hardcoded paths |
| T-01-07 | Information Disclosure | Footer copyright year | accept | Server-side Date.getFullYear() — no user data exposed |
| T-01-08 | Tampering | Header/Footer components | accept | Static Server Components with no user input — no injection vector |
</threat_model>

<verification>
- `bun run dev` serves homepage with visible header and footer
- Header displays "Road to Fi" brand name and Home/Blog links
- Footer displays copyright notice and Blog link
- Navigate to /blog (when created) — header and footer still present
- No TypeScript errors in Header.tsx or Footer.tsx
</verification>

<success_criteria>
1. Header component exists in components/layout/Header.tsx
2. Footer component exists in components/layout/Footer.tsx
3. Root layout imports and renders both Header and Footer
4. Header contains navigation links to / and /blog
5. Footer contains copyright text with dynamic year
6. All pages display header and footer through layout inheritance
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
