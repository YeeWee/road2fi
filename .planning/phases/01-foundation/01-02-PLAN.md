---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/globals.css
  - postcss.config.mjs
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "Tailwind CSS v4 is installed and configured via CSS-based @import"
    - "PostCSS plugin @tailwindcss/postcss is configured"
    - "Tailwind utilities compile correctly in the build"
  artifacts:
    - path: "app/globals.css"
      provides: "Tailwind v4 CSS import with @theme block"
      contains: "@import \"tailwindcss\""
    - path: "postcss.config.mjs"
      provides: "@tailwindcss/postcss plugin registration"
      contains: "@tailwindcss/postcss"
  key_links:
    - from: "app/globals.css"
      to: "postcss.config.mjs"
      via: "PostCSS processes @import"
      pattern: "@tailwindcss/postcss"
---

<objective>
Configure Tailwind CSS v4 with CSS-based configuration (no tailwind.config.js).

Purpose: Enable utility-first CSS styling for all project components using Tailwind v4's new CSS-based configuration approach.
Output: Updated app/globals.css with Tailwind import, updated postcss.config.mjs with @tailwindcss/postcss plugin
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@app/globals.css
@postcss.config.mjs
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind CSS v4 and PostCSS plugin with bun</name>
  <files>package.json</files>
  <read_first>
    - .planning/research/STACK.md (installation commands for Tailwind v4)
    - package.json (current dependencies)
  </read_first>
  <action>
    Run these bun commands to install Tailwind CSS v4:
    ```bash
    bun add tailwindcss @tailwindcss/postcss postcss
    bun add -d @tailwindcss/typography
    ```

    Verify package.json now contains:
    - "tailwindcss": "^4.x" in dependencies
    - "@tailwindcss/postcss": "^4.x" in dependencies
    - "postcss": "^8.x" in dependencies
    - "@tailwindcss/typography": "^0.5.x" in devDependencies
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); console.log(pkg.dependencies['tailwindcss']); console.log(pkg.dependencies['@tailwindcss/postcss']); console.log(pkg.dependencies['postcss'])"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains "tailwindcss" with version starting with "4"
    - package.json dependencies contains "@tailwindcss/postcss"
    - package.json dependencies contains "postcss"
  </acceptance_criteria>
  <done>
    Tailwind CSS v4, @tailwindcss/postcss, and postcss installed via bun. Typography plugin added for future Markdown rendering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure postcss.config.mjs and app/globals.css for Tailwind v4</name>
  <files>postcss.config.mjs, app/globals.css</files>
  <read_first>
    - .planning/research/STACK.md (postcss.config.mjs template, globals.css template)
    - postcss.config.mjs (current content — empty plugins object from Plan 01)
    - app/globals.css (current content — comment-only from Plan 01)
  </read_first>
  <action>
    Update postcss.config.mjs to register the Tailwind v4 PostCSS plugin:
    ```javascript
    export default {
      plugins: {
        "@tailwindcss/postcss": {},
      },
    }
    ```

    Update app/globals.css with Tailwind v4 CSS-based configuration:
    ```css
    @import "tailwindcss";

    @theme {
      --font-sans: var(--font-sans);
      --font-mono: var(--font-mono);
    }
    ```

    Note: Font CSS variables will be wired by Plan 05 (font configuration). The --font-sans and --font-mono variables here are placeholders that will be populated with actual next/font CSS variable names.
  </action>
  <verify>
    <automated>grep -q '@import "tailwindcss"' app/globals.css && grep -q "@theme" app/globals.css && grep -q "@tailwindcss/postcss" postcss.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - app/globals.css contains exactly: @import "tailwindcss"
    - app/globals.css contains "@theme" block
    - postcss.config.mjs contains "@tailwindcss/postcss" in plugins
    - postcss.config.mjs does NOT contain "tailwindcss" as a bare plugin name (must be @tailwindcss/postcss)
  </acceptance_criteria>
  <done>
    Tailwind v4 configured via CSS @import with @theme block. PostCSS plugin registered. Font variables are placeholders for Plan 05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build→output | Tailwind compiles CSS; no user input crosses boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | postcss.config.mjs | accept | Build configuration only; no runtime user input processed |
| T-01-05 | Information Disclosure | Compiled CSS output | accept | CSS is public-facing, no sensitive data in styles |
</threat_model>

<verification>
- `bun run build` completes without CSS compilation errors
- `bun run dev` serves pages with Tailwind utility classes applied
- No console warnings about missing Tailwind configuration
</verification>

<success_criteria>
1. Tailwind CSS v4 installed (package.json shows ^4.x)
2. app/globals.css contains `@import "tailwindcss"` and `@theme` block
3. postcss.config.mjs contains `@tailwindcss/postcss` plugin
4. Build completes without CSS errors
5. No tailwind.config.js file exists (v4 uses CSS-based config only)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
