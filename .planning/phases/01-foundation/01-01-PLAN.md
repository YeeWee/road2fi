---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.mjs
  - .gitignore
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - postcss.config.mjs
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "Project has a valid Next.js 16 package.json with correct dependencies"
    - "TypeScript is configured with proper compiler options"
    - "App Router directory structure exists (app/)"
    - "Development server starts without errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript compiler configuration"
      contains: "compilerOptions"
    - path: "next.config.mjs"
      provides: "Next.js configuration with ESM"
      exports: ["default"]
    - path: ".gitignore"
      provides: "Git ignore rules for Node.js/Next.js"
      contains: ".env"
    - path: "app/layout.tsx"
      provides: "Root layout component"
      exports: ["default", "metadata"]
    - path: "app/page.tsx"
      provides: "Homepage placeholder"
      exports: ["default"]
    - path: "app/globals.css"
      provides: "Global styles entry"
      contains: "@import"
    - path: "postcss.config.mjs"
      provides: "PostCSS configuration"
      contains: "plugins"
  key_links:
    - from: "package.json"
      to: "next.config.mjs"
      via: "moduleResolution and ESM"
      pattern: "type.*module"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
---

<objective>
Initialize Next.js 16 project with App Router, TypeScript, ESM module system, and basic file structure.

Purpose: Create the foundational project scaffold that all subsequent plans depend on.
Output: package.json, tsconfig.json, next.config.mjs, .gitignore, app/ directory with layout.tsx, page.tsx, globals.css, postcss.config.mjs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 16 project with bun and TypeScript</name>
  <files>package.json, tsconfig.json, .gitignore, next.config.mjs, postcss.config.mjs</files>
  <read_first>
    - .planning/research/STACK.md (installation commands, config templates)
    - CLAUDE.md (use bun, not npm)
  </read_first>
  <action>
    Run `bun create next-app@latest . --typescript --app --use-bun --yes` in the project root to scaffold Next.js 16 with App Router and TypeScript.

    After scaffolding completes, verify:
    1. package.json has "next": "^16.x" in dependencies
    2. package.json has "dev": "next dev" script (Turbopack default in v16)
    3. tsconfig.json exists with "strict": true
    4. .gitignore contains .env, .env.local, .next, node_modules

    Create next.config.mjs with ESM export:
    ```javascript
    /** @type {import('next').NextConfig} */
    const nextConfig = {}
    export default nextConfig
    ```

    Create postcss.config.mjs (will be populated by Tailwind plan, but file must exist):
    ```javascript
    export default {
      plugins: {},
    }
    ```

    Ensure .gitignore includes:
    - .env
    - .env.local
    - .env.*
    - .next
    - node_modules
    - *.tsbuildinfo
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); console.log(pkg.dependencies.next); console.log(pkg.scripts.dev)" && grep -q "\.env" .gitignore && grep -q "strict" tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" in dependencies with version "16" or "^16"
    - package.json scripts.dev contains "next dev"
    - tsconfig.json contains "strict": true
    - .gitignore contains ".env" (bare `grep -c "^\.env$" .gitignore` returns >= 1)
    - next.config.mjs contains "export default"
    - postcss.config.mjs exists and contains "plugins"
  </acceptance_criteria>
  <done>
    Next.js 16 project initialized with TypeScript, ESM, App Router. Development server can start. .gitignore protects secrets.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create app directory structure with root layout and homepage</name>
  <files>app/layout.tsx, app/page.tsx, app/globals.css</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (directory structure, root layout pattern)
    - app/layout.tsx (if exists from create-next-app)
    - app/page.tsx (if exists from create-next-app)
    - app/globals.css (if exists from create-next-app)
  </read_first>
  <action>
    Create or update app/layout.tsx as the root layout component with:
    ```tsx
    import type { Metadata } from "next"
    import "./globals.css"

    export const metadata: Metadata = {
      title: "Road to Fi — Financial Independence",
      description: "Helping ordinary people achieve Financial Independence through clear, actionable investment insights.",
    }

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <html lang="en">
          <body>{children}</body>
        </html>
      )
    }
    ```

    Create app/page.tsx as a homepage placeholder:
    ```tsx
    export default function Home() {
      return (
        <main>
          <h1>Road to Fi</h1>
          <p>Financial Independence, Retire Early</p>
        </main>
      )
    }
    ```

    Create app/globals.css as a minimal styles entry (Tailwind import will be added in Plan 02):
    ```css
    /* Global styles — Tailwind v4 will be configured in next step */
    ```
  </action>
  <verify>
    <automated>grep -q "RootLayout" app/layout.tsx && grep -q 'lang="en"' app/layout.tsx && grep -q "globals.css" app/layout.tsx && grep -q "Home" app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx contains "export const metadata"
    - app/layout.tsx contains 'lang="en"'
    - app/layout.tsx contains 'import "./globals.css"'
    - app/layout.tsx contains "export default function RootLayout"
    - app/page.tsx contains "export default function Home"
    - app/globals.css exists
  </acceptance_criteria>
  <done>
    Root layout exports metadata and renders html/body with lang="en". Homepage placeholder renders. globals.css exists for Tailwind configuration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→repo | Source code committed to git; .gitignore protects .env files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env files | mitigate | .gitignore explicitly lists .env, .env.local, .env.* to prevent credential leaks |
| T-01-02 | Tampering | next.config.mjs | accept | Configuration file, no user input processed; low-value target |
| T-01-03 | Information Disclosure | package.json scripts | accept | Build scripts are public, no secrets in npm scripts |
</threat_model>

<verification>
- `bun run dev` starts without errors (exit code 0 within 10 seconds)
- `bun run build` completes without TypeScript errors
- `npx tsc --noEmit` reports no errors
- app/layout.tsx, app/page.tsx, app/globals.css all exist and export correctly
</verification>

<success_criteria>
1. package.json has Next.js 16 with App Router enabled
2. TypeScript configured with strict mode
3. app/ directory exists with layout.tsx, page.tsx, globals.css
4. .gitignore protects .env files
5. Development server starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
