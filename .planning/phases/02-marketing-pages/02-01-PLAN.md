---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [components/home/Hero.tsx, app/globals.css]
autonomous: true
requirements: [HOME-01]

must_haves:
  truths:
    - "User sees bold headline as first content on page"
    - "User sees a clear call-to-action button"
    - "Hero section fills full or near-full viewport height"
  artifacts:
    - path: "components/home/Hero.tsx"
      provides: "Hero section rendering"
      min_lines: 20
    - path: "app/globals.css"
      provides: "CTA button animation styles"
      contains: "@keyframes fadeIn"
  key_links:
    - from: "components/home/Hero.tsx"
      to: "app/globals.css"
      via: "Tailwind animation class"
      pattern: "animate-fadeIn"
---

<objective>
Build the homepage hero section with bold headline, subheadline, and call-to-action button.
Per locked decisions D-01 (bold headline + CTA) and D-02 (full/near-full viewport height).

Purpose: Create immediate visual impact that communicates Road to Fi's value proposition and guides users toward the blog.
Output: Hero component and supporting CSS animation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketing-pages/02-CONTEXT.md
@app/page.tsx
@app/globals.css
@components/layout/Header.tsx
@components/layout/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero component</name>
  <files>components/home/Hero.tsx</files>
  <read_first>
    - components/home/Hero.tsx (creates new file)
    - app/globals.css (for available theme tokens)
    - components/layout/Header.tsx (for consistent styling patterns)
    - .planning/phases/02-marketing-pages/02-CONTEXT.md (D-01, D-02 decisions)
  </read_first>
  <action>
    Create `components/home/Hero.tsx` with a default export React component. The component MUST contain:

    1. A `<section>` wrapper with classes: `relative flex min-h-[calc(100vh-4rem)] flex-col items-center justify-center overflow-hidden bg-white`
       (min-h accounts for ~4rem header height, giving near-full viewport feel per D-02)

    2. Inside the section, a centered content container: `<div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 text-center">`

    3. The headline: `<h1 className="text-5xl sm:text-6xl lg:text-7xl font-bold tracking-tight text-gray-900 mb-6">Your Road to Financial Independence Starts Here</h1>`
       Per D-01: bold, high-impact marketing site feel with large headline

    4. Subheadline: `<p className="text-xl sm:text-2xl text-gray-600 mb-10 max-w-2xl mx-auto">Clear, actionable investment insights to help ordinary people build lasting wealth.</p>`

    5. CTA button:
       ```tsx
       <Link href="/blog" className="inline-block rounded-lg bg-blue-600 px-8 py-4 text-lg font-semibold text-white shadow-lg transition-all hover:bg-blue-700 hover:shadow-xl hover:-translate-y-0.5">
         Start Your Journey
       </Link>
       ```
       CTA links to /blog (primary engagement action per CONTEXT.md specifics)

    6. Below the CTA, a subtle scroll hint:
       ```tsx
       <div className="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
         <svg className="w-6 h-6 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
           <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 14l-7 7m0 0l-7-7m7 7V3" />
         </svg>
       </div>
       ```

    7. Add `import Link from "next/link"` at the top of the file.

    Use default export. Do NOT use named exports.
  </action>
  <acceptance_criteria>
    - components/home/Hero.tsx exists with default export
    - File contains text "Your Road to Financial Independence starts Here" (case-insensitive grep)
    - File contains `min-h-[calc(100vh-4rem)]` (viewport height per D-02)
    - File contains `<Link href="/blog"` (CTA links to blog)
    - File contains `bg-blue-600` (CTA button color)
    - File contains `animate-bounce` (scroll hint animation)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'default export' components/home/Hero.tsx && grep -qi 'road to financial independence' components/home/Hero.tsx && grep -q 'min-h-\[calc(100vh-4rem)\]' components/home/Hero.tsx && grep -q 'href="/blog"' components/home/Hero.tsx</automated>
  </verify>
  <done>Hero component created with bold headline, subheadline, CTA button linking to /blog, and scroll-down hint. Component uses default export and follows established Tailwind v4 patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS animation and section polish</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (current Tailwind v4 theme configuration)
  </read_first>
  <action>
    Append to `app/globals.css` after the existing `@theme` block:

    ```css
    /* Hero section animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(1rem);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fadeIn {
      animation: fadeIn 0.8s ease-out forwards;
    }

    .animate-fadeIn-delay {
      animation: fadeIn 0.8s ease-out 0.2s forwards;
      opacity: 0;
    }
    ```

    These classes support the fadeIn animation for hero content entrance. The Hero component does NOT need to use these immediately — they are available for enhancement.
  </action>
  <acceptance_criteria>
    - app/globals.css contains @keyframes fadeIn definition
    - app/globals.css contains .animate-fadeIn class
    - app/globals.css contains .animate-fadeIn-delay class
    - Original @import "tailwindcss" and @theme block remain unchanged
  </acceptance_criteria>
  <verify>
    <automated>grep -q '@keyframes fadeIn' app/globals.css && grep -q '\.animate-fadeIn' app/globals.css && grep -q '\.animate-fadeIn-delay' app/globals.css</automated>
  </verify>
  <done>CSS animations added to globals.css: fadeIn keyframes, animate-fadeIn, and animate-fadeIn-delay classes. Original Tailwind import and @theme block preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→link | CTA button navigates to /blog; no user input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Hero.tsx Link | accept | Static Next.js Link component; no external URLs or user-controlled hrefs |
| T-02-02 | Tampering | globals.css | accept | Static CSS with no runtime injection; build-time only |
</threat_model>

<verification>
- Hero component renders without TypeScript errors
- Hero section occupies full viewport minus header height on desktop
- CTA button is visible and links to /blog
- Scroll hint arrow animates with bounce
</verification>

<success_criteria>
- Hero.tsx created with default export containing headline, subheadline, CTA, scroll hint
- CTA button uses blue-600 background with hover effects (blue-700, shadow-xl, -translate-y-0.5)
- Section uses min-h-[calc(100vh-4rem)] for near-full viewport height (D-02)
- fadeIn CSS animations available in globals.css
- No TypeScript errors after component creation
</success_criteria>

<output>
After completion, create `.planning/phases/02-marketing-pages/02-01-SUMMARY.md`
</output>
