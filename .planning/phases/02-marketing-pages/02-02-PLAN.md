---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [components/home/Philosophy.tsx]
autonomous: true
requirements: [HOME-02]

must_haves:
  truths:
    - "User sees 3-4 value investing principle cards in a grid"
    - "Each card displays a principle name, icon/number, and short description"
    - "Cards are centered within max-w-5xl content width"
  artifacts:
    - path: "components/home/Philosophy.tsx"
      provides: "Philosophy section with card grid"
      min_lines: 40
  key_links:
    - from: "components/home/Philosophy.tsx"
      to: "app/globals.css"
      via: "Tailwind utility classes"
      pattern: "grid|gap-8|card"
---

<objective>
Build the investment philosophy section with 3-4 value investing principle cards in a responsive grid.
Per locked decisions D-03 (card grid with principle name, icon/number, description) and D-04 (align with max-w-5xl content width).

Purpose: Showcase the firm's core investment principles in a scannable, professional format that builds credibility.
Output: Philosophy section component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketing-pages/02-CONTEXT.md
@components/layout/Header.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Philosophy section with card grid</name>
  <files>components/home/Philosophy.tsx</files>
  <read_first>
    - components/home/Philosophy.tsx (creates new file)
    - components/layout/Header.tsx (for consistent styling patterns, default export)
    - .planning/phases/02-marketing-pages/02-CONTEXT.md (D-03, D-04 decisions)
  </read_first>
  <action>
    Create `components/home/Philosophy.tsx` with a default export React component containing:

    1. A `<section>` wrapper with classes: `py-20 sm:py-24 bg-gray-50`

    2. Section header:
       ```tsx
       <div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 mb-16 text-center">
         <h2 className="text-3xl sm:text-4xl font-bold tracking-tight text-gray-900 mb-4">Our Investment Philosophy</h2>
         <p className="text-lg text-gray-600 max-w-2xl mx-auto">Time-tested principles that guide every decision we make.</p>
       </div>
       ```

    3. Card grid container:
       ```tsx
       <div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
         <div className="grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-4">
       ```
       (Responsive: 1 column on mobile, 2 on tablet, 4 on desktop — per D-03)

    4. Four principle cards, each with this structure:
       ```tsx
       <div className="rounded-xl border border-gray-200 bg-white p-8 shadow-sm transition-shadow hover:shadow-md">
         <div className="mb-4 flex h-12 w-12 items-center justify-center rounded-lg bg-blue-600 text-white font-bold text-lg">
           1
         </div>
         <h3 className="text-xl font-semibold text-gray-900 mb-3">Principle Name</h3>
         <p className="text-gray-600">Description text here.</p>
       </div>
       ```

    The four principles (per D-03, real value investing principles):

    | # | Name | Description |
    |---|------|-------------|
    | 1 | Invest in What You Understand | Only buy businesses you can analyze and comprehend. If you can't explain how a company makes money, don't own it. |
    | 2 | Margin of Safety | Always buy assets below their intrinsic value. This buffer protects against mistakes, bad luck, and market volatility. |
    | 3 | Think Long-Term | Patience is the investor's greatest edge. Compound returns accelerate over decades, not months. |
    | 4 | Let Compounding Work | Reinvest earnings and hold quality businesses. Time in the market beats timing the market. |

    5. Close all divs properly. Use default export.
  </action>
  <acceptance_criteria>
    - components/home/Philosophy.tsx exists with default export
    - File contains "Our Investment Philosophy" heading text
    - File contains 4 card elements with numbered circles (bg-blue-600, h-12, w-12)
    - File contains "Invest in What You Understand" (principle 1)
    - File contains "Margin of Safety" (principle 2)
    - File contains "Think Long-Term" (principle 3)
    - File contains "Let Compounding Work" (principle 4)
    - File contains `grid-cols-1` and `sm:grid-cols-2` and `lg:grid-cols-4` (responsive grid per D-03)
    - File contains `max-w-5xl` (content width per D-04)
    - Each card has rounded-xl border border-gray-200 bg-white p-8
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'default export' components/home/Philosophy.tsx && grep -q 'Our Investment Philosophy' components/home/Philosophy.tsx && grep -q 'Invest in What You Understand' components/home/Philosophy.tsx && grep -q 'Margin of Safety' components/home/Philosophy.tsx && grep -q 'Think Long-Term' components/home/Philosophy.tsx && grep -q 'Let Compounding Work' components/home/Philosophy.tsx && grep -q 'lg:grid-cols-4' components/home/Philosophy.tsx && grep -q 'max-w-5xl' components/home/Philosophy.tsx</automated>
  </verify>
  <done>Philosophy component created with 4 value investing principle cards in responsive grid. Each card has numbered circle, title, and description. Component uses default export and max-w-5xl content width.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→static | All content is static text; no user input or external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Philosophy.tsx | accept | Static content only; no user input, no data fetching, no external API calls |
| T-02-04 | Injection | Philosophy.tsx | accept | Hardcoded text content; no string interpolation from untrusted sources |
</threat_model>

<verification>
- Philosophy component renders 4 cards in responsive grid (1/2/4 columns)
- Each card displays principle number, name, and description
- Cards use hover:shadow-md transition effect
- Section background is gray-50 for visual contrast
</verification>

<success_criteria>
- Philosophy.tsx created with default export containing 4 principle cards
- Card grid responsive: 1 col mobile, 2 col tablet, 4 col desktop
- Each card has numbered blue circle (bg-blue-600), title (text-xl font-semibold), description (text-gray-600)
- Section uses max-w-5xl content width (D-04)
- No TypeScript errors after component creation
</success_criteria>

<output>
After completion, create `.planning/phases/02-marketing-pages/02-02-SUMMARY.md`
</output>
