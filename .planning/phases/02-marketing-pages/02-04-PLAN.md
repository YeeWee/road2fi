---
phase: 02
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified: [components/layout/Header.tsx, components/layout/Footer.tsx, app/page.tsx]
autonomous: true
requirements: [HOME-04]

must_haves:
  truths:
    - "Header shows site branding and navigation links on all pages"
    - "Footer shows copyright, blog link, and essential links on all pages"
    - "Homepage displays all three sections stacked: Hero, Philosophy, About Us"
  artifacts:
    - path: "components/layout/Header.tsx"
      provides: "Enhanced navigation header"
      min_lines: 15
    - path: "components/layout/Footer.tsx"
      provides: "Enhanced footer with contact info and links"
      min_lines: 20
    - path: "app/page.tsx"
      provides: "Homepage assembling all sections"
      min_lines: 10
  key_links:
    - from: "app/page.tsx"
      to: "components/home/Hero.tsx"
      via: "import and render"
      pattern: "import.*Hero"
    - from: "app/page.tsx"
      to: "components/home/Philosophy.tsx"
      via: "import and render"
      pattern: "import.*Philosophy"
    - from: "app/page.tsx"
      to: "components/home/About.tsx"
      via: "import and render"
      pattern: "import.*About"
---

<objective>
Enhance header and footer components in place, and assemble the homepage from all three section components.
Per locked decisions D-07 (enhance in place), D-08 (header links), D-09 (footer contact info), D-10 (single stacked page), D-11 (replace placeholder).

Purpose: Complete the marketing pages by wiring together all components and polishing global navigation.
Output: Enhanced Header, enhanced Footer, and assembled homepage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-marketing-pages/02-CONTEXT.md
@.planning/phases/02-marketing-pages/02-01-PLAN.md
@.planning/phases/02-marketing-pages/02-02-PLAN.md
@.planning/phases/02-marketing-pages/02-03-PLAN.md
@components/layout/Header.tsx
@components/layout/Footer.tsx
@app/page.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Header component with styling and links</name>
  <files>components/layout/Header.tsx</files>
  <read_first>
    - components/layout/Header.tsx (current component to enhance)
    - .planning/phases/02-marketing-pages/02-CONTEXT.md (D-07, D-08 decisions)
  </read_first>
  <action>
    Modify `components/layout/Header.tsx` IN PLACE (per D-07 — enhance, don't replace). Update the existing component to:

    1. Keep the existing structure: `<header>` with `<nav>` inside, max-w-5xl, border-b, bg-white
    2. Add `sticky top-0 z-50` classes to the `<header>` element so navigation stays visible on scroll
    3. Keep the "Road to Fi" brand link on the left as-is (already correct)
    4. Keep existing Home and Blog links — add a subtle "About" link that scrolls to the About section:
       ```tsx
       <a href="#about" className="hover:text-gray-900">About</a>
       ```
       (Use `<a>` with href="#about" for in-page anchor, not Next.js Link since it's a same-page scroll)
    5. Update nav link container styling to: `flex gap-6 sm:gap-8 text-sm text-gray-600`

    Final expected structure:
    ```tsx
    import Link from "next/link"

    export default function Header() {
      return (
        <header className="sticky top-0 z-50 border-b border-gray-200 bg-white">
          <nav className="mx-auto flex max-w-5xl items-center justify-between p-4 sm:px-6 lg:px-8">
            <Link href="/" className="text-xl font-bold tracking-tight text-gray-900">
              Road to Fi
            </Link>
            <div className="flex gap-6 sm:gap-8 text-sm text-gray-600">
              <Link href="/" className="hover:text-gray-900">
                Home
              </Link>
              <Link href="/blog" className="hover:text-gray-900">
                Blog
              </Link>
              <a href="#about" className="hover:text-gray-900">
                About
              </a>
            </div>
          </nav>
        </header>
      )
    }
    ```
  </action>
  <acceptance_criteria>
    - components/layout/Header.tsx contains `sticky top-0 z-50` in header className
    - components/layout/Header.tsx contains `<a href="#about"` (About anchor link)
    - components/layout/Header.tsx contains `href="/blog"` (Blog link preserved)
    - components/layout/Header.tsx contains `href="/"` (Home link preserved)
    - File still uses default export
    - File still has "Road to Fi" brand text
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'sticky top-0 z-50' components/layout/Header.tsx && grep -q 'href="#about"' components/layout/Header.tsx && grep -q 'href="/blog"' components/layout/Header.tsx && grep -q 'default export' components/layout/Header.tsx</automated>
  </verify>
  <done>Header enhanced with sticky positioning, z-50 stacking, and About anchor link. Original Home/Blog links and brand preserved. Component still uses default export.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Footer component with contact info and additional links</name>
  <files>components/layout/Footer.tsx</files>
  <read_first>
    - components/layout/Footer.tsx (current component to enhance)
    - .planning/phases/02-marketing-pages/02-CONTEXT.md (D-07, D-09 decisions)
  </read_first>
  <action>
    Modify `components/layout/Footer.tsx` IN PLACE (per D-07 — enhance, don't replace). Update the existing component to:

    1. Keep the existing structure: `<footer>` with border-t, bg-gray-50, max-w-5xl
    2. Restructure into three sections: top (link columns), bottom (copyright + blog link)

    Final expected structure:
    ```tsx
    import Link from "next/link"

    export default function Footer() {
      const year = new Date().getFullYear()
      return (
        <footer className="border-t border-gray-200 bg-gray-50">
          <div className="mx-auto max-w-5xl px-4 py-12 sm:px-6 lg:px-8">
            <div className="grid grid-cols-1 gap-8 sm:grid-cols-3 mb-8">
              {/* Brand column */}
              <div>
                <h3 className="text-lg font-semibold text-gray-900 mb-2">Road to Fi</h3>
                <p className="text-sm text-gray-600">Helping ordinary people achieve Financial Independence.</p>
              </div>
              {/* Navigation column */}
              <div>
                <h4 className="text-sm font-semibold text-gray-900 mb-2">Navigate</h4>
                <ul className="space-y-2 text-sm text-gray-600">
                  <li><Link href="/" className="hover:text-gray-900">Home</Link></li>
                  <li><Link href="/blog" className="hover:text-gray-900">Blog</Link></li>
                  <li><a href="#about" className="hover:text-gray-900">About</a></li>
                </ul>
              </div>
              {/* Contact column */}
              <div>
                <h4 className="text-sm font-semibold text-gray-900 mb-2">Contact</h4>
                <p className="text-sm text-gray-600">hello@roadtofi.com</p>
              </div>
            </div>
            {/* Bottom bar */}
            <div className="flex flex-col items-center gap-4 border-t border-gray-200 pt-8 text-sm text-gray-600 sm:flex-row sm:justify-between">
              <p>&copy; {year} Road to Fi. All rights reserved.</p>
              <div className="flex gap-4">
                <Link href="/blog" className="hover:text-gray-900">
                  Blog
                </Link>
              </div>
            </div>
          </div>
        </footer>
      )
    }
    ```

    Key changes from original:
    - py-8 → py-12 for more padding
    - Added 3-column grid layout (brand, navigation, contact)
    - Brand description added
    - Navigation links: Home, Blog, About
    - Contact email: hello@roadtofi.com (placeholder, D-09)
    - Bottom bar separated with border-t and pt-8
  </action>
  <acceptance_criteria>
    - components/layout/Footer.tsx contains 3-column grid with `sm:grid-cols-3`
    - File contains "Road to Fi" brand heading
    - File contains "Helping ordinary people achieve Financial Independence" tagline
    - File contains navigation links: Home, Blog, About
    - File contains contact email "hello@roadtofi.com"
    - File contains copyright with `{year}` variable
    - File still uses default export
    - File still has `border-t border-gray-200 bg-gray-50` on footer element
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'sm:grid-cols-3' components/layout/Footer.tsx && grep -q 'hello@roadtofi.com' components/layout/Footer.tsx && grep -q 'default export' components/layout/Footer.tsx && grep -q 'All rights reserved' components/layout/Footer.tsx</automated>
  </verify>
  <done>Footer enhanced with 3-column layout (brand, navigation, contact), tagline, email address, and bottom bar with copyright. Original structure preserved. Component still uses default export.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble homepage from all section components</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current placeholder to replace per D-11)
    - components/home/Hero.tsx (created by plan 02-01)
    - components/home/Philosophy.tsx (created by plan 02-02)
    - components/home/About.tsx (created by plan 02-03)
  </read_first>
  <action>
    Replace `app/page.tsx` entirely (per D-11 — replace the current placeholder). The new homepage imports and stacks all three section components in order (per D-10, D-06):

    ```tsx
    import Hero from "@/components/home/Hero"
    import Philosophy from "@/components/home/Philosophy"
    import About from "@/components/home/About"

    export default function Home() {
      return (
        <>
          <Hero />
          <Philosophy />
          <About />
        </>
      )
    }
    ```

    Key details:
    - Use `@/` path alias for imports (established pattern from Phase 1)
    - Use React Fragment `<>...</>` as wrapper (no extra `<main>` since layout.tsx already wraps children in `<main>`)
    - Section order is critical: Hero → Philosophy → About (per D-06, D-10)
    - No additional wrapping elements, no extra styling on the page itself
  </action>
  <acceptance_criteria>
    - app/page.tsx contains `import Hero from "@/components/home/Hero"`
    - app/page.tsx contains `import Philosophy from "@/components/home/Philosophy"`
    - app/page.tsx contains `import About from "@/components/home/About"`
    - app/page.tsx contains `<Hero />` before `<Philosophy />` before `<About />`
    - app/page.tsx does NOT contain placeholder text "Road to Fi" as h1 (old content removed)
    - app/page.tsx does NOT contain `<main>` tag (layout already provides it)
    - File uses default export
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'import Hero from "@/components/home/Hero"' app/page.tsx && grep -q 'import Philosophy from "@/components/home/Philosophy"' app/page.tsx && grep -q 'import About from "@/components/home/About"' app/page.tsx && grep -q '<Hero />' app/page.tsx && grep -q '<Philosophy />' app/page.tsx && grep -q '<About />' app/page.tsx</automated>
  </verify>
  <done>Homepage assembled with all three sections imported and stacked in correct order: Hero, Philosophy, About. Placeholder content removed. Uses Fragment wrapper since layout.tsx provides main element.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→anchor | About link uses #about anchor — scrolls to element with id="about" |
| client→external | Footer displays email address — no mailto: link to prevent spam harvesting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Spoofing | Header.tsx | mitigate | All links use Next.js Link for internal routes; only About uses #anchor (same-page, trusted) |
| T-02-08 | Information Disclosure | Footer.tsx (email) | accept | Static email address displayed as plain text (no mailto:); low-value target, no PII beyond public contact |
| T-02-09 | Tampering | page.tsx | accept | Static component imports; no user input or dynamic rendering |
</threat_model>

<verification>
- Header is sticky and stays visible on scroll
- Header contains Home, Blog, and About links
- Footer displays brand, tagline, navigation links, and contact email
- Footer copyright shows current year
- Homepage renders three sections in order: Hero → Philosophy → About
- No TypeScript compilation errors
- `npm run build` completes without errors
</verification>

<success_criteria>
- Header.tsx enhanced with sticky positioning and About anchor link (D-07, D-08)
- Footer.tsx enhanced with 3-column layout, tagline, email, navigation links (D-07, D-09)
- page.tsx replaces placeholder with assembled sections in correct order (D-10, D-11)
- All three homepage sections visible when visiting /
- No TypeScript errors across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/02-marketing-pages/02-04-SUMMARY.md`
</output>
